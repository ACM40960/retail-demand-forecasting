# Perishable Demand Forecasting & Zero-Waste Inventory Engine - Python dependencies.

# --- core data stack ---
numpy==2.3.2
pandas==2.3.1
pyarrow==21.0.0        # >=21.0.0 REQUIRED by datasets 4.8.5 - do not downgrade

# --- figures (src/utils/plots.py) ---
matplotlib==3.10.8

# --- baselines (src/baselines.py) ---
xgboost==3.3.0         # quantile baseline
statsmodels==0.14.5    # SARIMA baseline

# --- recovery (src/recovery.py) ---
lightgbm==4.6.0        # Stage-1 hourly demand-rate model
scikit-learn==1.8.0    # train_test_split

# --- forecasting (src/forecast.py) ---
# Notebook 02 only, and the one stage worth a GPU. Left unpinned: Colab ships its own CUDA-matched
# torch, and pinning it here forces a multi-GB reinstall that can end up on the wrong CUDA build.
# Versions verified locally: torch 2.13.0, lightning 2.6.5, pytorch-forecasting 1.8.0.
torch
lightning
pytorch-forecasting

# --- data source + notebook ---
datasets==4.8.5        # Hugging Face FreshRetailNet-50K loader (src/utils/data_io.py)
ipykernel==7.1.0       # kernel for notebooks/
